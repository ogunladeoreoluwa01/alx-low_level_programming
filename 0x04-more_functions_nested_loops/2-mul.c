#include <stdio.h>
#include "main.h"
/**
 * mul - multiplies two digits
 * Description:'a function'
 * Return: the product of a and b
 * @a: one of the operands
 * @b: one of the operands
 */
int mul(int a, int b)
{
	int z = a * b;

	return (z);
}
