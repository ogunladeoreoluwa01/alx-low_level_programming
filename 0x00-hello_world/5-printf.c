#include <stdio.h>
/**
 *main - it is the entry point for the program
 *Description:'Prints a phrase using the printf function'
 *Return: 0 if properly executed else a non zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
