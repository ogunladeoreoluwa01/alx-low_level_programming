#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers
 * Return: 'z' if succes
 * @c: integer to be added
 * @d: integer to be added
 */
int add(int c, int d)
{
	int z = c + d;

	return (z);
}
